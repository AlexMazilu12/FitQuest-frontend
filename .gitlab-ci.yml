image: cypress/base:14.17.0

variables:
  GIT_DEPTH: "0"

cache:
  paths:
    - node_modules/

stages:
  - build
  - test

build:
  stage: build
  script:
    - npm install
    - npm run build

cypress-test:
  stage: test
  script:
    - npm run dev
    - npx cypress run
  dependencies:
    - build